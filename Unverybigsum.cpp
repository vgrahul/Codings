#include<iostream.h>
#include<math.h>
using namespace std;
int main)()
{
	long a[9];
	int N = 9;
	while(N)
	{
	scanf("%d",&a[i]);
    --N;
    }
    while(N)
    {
    if(a[i]<=exp(10,10)&&a[i]>=0)
    }
